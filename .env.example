# Option 1: direct connection URL (recommended)
DATABASE_URL=postgresql://user:password@host:5432/database

# Option 2: Railway-style split variables (used when DATABASE_URL is not set)
# PGHOST=host
# PGPORT=5432
# PGUSER=user
# PGPASSWORD=password
# PGDATABASE=database
